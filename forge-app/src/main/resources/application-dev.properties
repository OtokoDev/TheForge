# Profil "dev" — activé par défaut au lancement (voir Application.java).
# Connexion à la base PostgreSQL locale créée par scripts/create-database.sql.
datasources.default.url=jdbc:postgresql://localhost:5432/theforge
datasources.default.username=postgres
datasources.default.password=postgres
